# Spec: M1.10-splash-tabs — Tabs base (Inicio, Catálogo, Carrito, Cuenta) con Ionicons

**Status**: APPROVED (auto-aprobado)
**Spec ID**: 2026-09-10-m1-10-splash-tabs
**Author**: Agente principal (Architect)
**Date**: 2026-09-10
**Banda**: Tier 3 — UI base
**Issue GitHub**: #11 [101M][F1] M1.5 — Splash, guards, tabs y navegación base
**Decisión de producto**: Splash ya está completo (M1.5 + M1.8 Montserrat). Esta spec se enfoca en **completar las 4 tabs** (Inicio ya existe, faltan Catálogo/Carrito/Cuenta) con iconos Ionicons.

---

## Contexto

Issue #11: Splash, guards, tabs y navegación base. Estado actual:
- ✅ Splash COMPLETO (M1.5 wiring + M1.8 Montserrat font-loading).
- ✅ Auth guards COMPLETOS (M1.2 + M1.5-session-cycle).
- 🚧 Tabs solo tiene `index.tsx` placeholder. Faltan `catalog.tsx`, `cart.tsx`, `account.tsx`.
- 🚧 `(tabs)/_layout.tsx` solo declara 1 `Tabs.Screen`.
- 🚧 NO hay librería de iconos declarada.

## Objetivo

1. **`@expo/vector-icons`** agregado como dep directa (ya está transitivo en SDK 57).
2. **`(tabs)/_layout.tsx`** declara 4 `Tabs.Screen` (Inicio, Catálogo, Carrito, Cuenta) con `title` + `tabBarIcon` (Ionicons).
3. **3 placeholders nuevos:** `catalog.tsx`, `cart.tsx`, `account.tsx` — cada uno muestra "Próximamente — Fase X".
4. **`index.tsx` enriquecido** con bienvenida básica (sin API todavía; consume estático).
5. **Tests BDD** (≥8) que cubren declaraciones de tabs, iconos, placeholders, authGuard.

## Fuera de alcance

- Lógica real de cada tab (F2 catálogo, F3 cart, F6 perfil).
- Native tabs (`react-native-bottom-tabs`).
- Universal Links / App Links (F7).
- Persistencia de tab activa (default behavior de expo-router).
- Theme switching / dark mode.
- Tests visuales con snapshots de iconos (font-based, requiere device).

## Arquitectura afectada

### Crear

- `src/app/(tabs)/catalog.tsx` (~30 líneas, placeholder)
- `src/app/(tabs)/cart.tsx` (~30 líneas, placeholder)
- `src/app/(tabs)/account.tsx` (~60 líneas, placeholder con info usuario + logout button si hay sesión)
- `src/app/(tabs)/__tests__/_layout.spec.tsx` (~5 tests)
- `src/app/(tabs)/__tests__/catalog.spec.tsx` (~2 tests smoke)
- `src/app/(tabs)/__tests__/cart.spec.tsx` (~2 tests smoke)
- `src/app/(tabs)/__tests__/account.spec.tsx` (~3 tests, incluye logout si hay sesión)

### Modificar

- `package.json` — añadir `"@expo/vector-icons": "^14.x"` a dependencies
- `src/app/(tabs)/_layout.tsx` — declarar las 4 `Tabs.Screen` con title + tabBarIcon
- `src/app/(tabs)/index.tsx` — enriquecer con bienvenida (sin API)
- `src/types/node-test.d.ts` — añadir tipo `Ionicons` si jest.setup.js lo necesita (ver § Mocking)
- `jest.setup.js` — añadir mock de `@expo/vector-icons` (ver § Mocking)

### NO tocar

- `src/app/_layout.tsx` (raíz completo).
- `(auth)/_layout.tsx` ni pantallas (auth).
- Backend Laravel.
- Otras deps.

## Contratos

### Dependencia

```json
{
  "dependencies": {
    "@expo/vector-icons": "^14.1.0"
  }
}
```

(Verificar versión exacta leyendo `pnpm-lock.yaml` para la transitiva actual y bumpear a directa.)

### `(tabs)/_layout.tsx`

```tsx
import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { Redirect } from 'expo-router';
import { useAuthStore } from '@/stores/auth-store';
import { brandColors } from '@/theme/tokens';

export default function TabsLayout() {
  const isAuthenticated = useAuthStore((s) => s.isAuthenticated);
  const isHydrated = useAuthStore((s) => s.isHydrated);
  
  if (!isHydrated) return null;
  if (!isAuthenticated) return <Redirect href="/(auth)/login" />;

  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: brandColors.primary,
        tabBarInactiveTintColor: brandColors.dark,
        tabBarStyle: { backgroundColor: brandColors.medium },
        headerStyle: { backgroundColor: brandColors.primary },
        headerTintColor: 'white',
        headerTitleStyle: { fontFamily: 'Montserrat-Bold', fontWeight: '700' },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: 'Inicio',
          tabBarIcon: ({ color, focused }) => (
            <Ionicons name={focused ? 'home' : 'home-outline'} size={24} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="catalog"
        options={{
          title: 'Catálogo',
          tabBarIcon: ({ color, focused }) => (
            <Ionicons name={focused ? 'grid' : 'grid-outline'} size={24} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="cart"
        options={{
          title: 'Carrito',
          tabBarIcon: ({ color, focused }) => (
            <Ionicons name={focused ? 'cart' : 'cart-outline'} size={24} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="account"
        options={{
          title: 'Cuenta',
          tabBarIcon: ({ color, focused }) => (
            <Ionicons name={focused ? 'person' : 'person-outline'} size={24} color={color} />
          ),
        }}
      />
    </Tabs>
  );
}
```

### Placeholders

#### `catalog.tsx`

```tsx
import { View, Text } from 'react-native';

export default function CatalogTab() {
  return (
    <View className="flex-1 items-center justify-center bg-brand-medium p-6">
      <Text className="font-brand-bold text-2xl text-brand-dark mb-2">Catálogo</Text>
      <Text className="font-brand text-base text-brand-dark text-center">
        Próximamente — Fase 2
      </Text>
    </View>
  );
}
```

#### `cart.tsx`

```tsx
import { View, Text } from 'react-native';

export default function CartTab() {
  return (
    <View className="flex-1 items-center justify-center bg-brand-medium p-6">
      <Text className="font-brand-bold text-2xl text-brand-dark mb-2">Carrito</Text>
      <Text className="font-brand text-base text-brand-dark text-center">
        Próximamente — Fase 3
      </Text>
    </View>
  );
}
```

#### `account.tsx`

```tsx
import { View, Text, Pressable, ActivityIndicator } from 'react-native';
import { router } from 'expo-router';
import { useAuthStore } from '@/stores/auth-store';
import { authService } from '@/core/services/auth-service';

export default function AccountTab() {
  const user = useAuthStore((s) => s.user);
  const clearSession = useAuthStore((s) => s.clearSession);
  const [loggingOut, setLoggingOut] = useState(false);

  const handleLogout = async () => {
    setLoggingOut(true);
    try {
      await authService.logout();
      await clearSession();
      router.replace('/(auth)/login');
    } catch (err) {
      if (__DEV__) console.warn('[account] logout failed', err);
      // Continuar con clear local
      await clearSession();
      router.replace('/(auth)/login');
    } finally {
      setLoggingOut(false);
    }
  };

  return (
    <View className="flex-1 bg-brand-medium p-6">
      <Text className="font-brand-bold text-2xl text-brand-dark mb-4">Mi cuenta</Text>
      {user && (
        <View className="bg-white rounded-lg p-4 mb-6">
          <Text className="font-brand-bold text-base text-brand-dark">{user.name}</Text>
          <Text className="font-brand text-sm text-brand-dark/70">{user.email}</Text>
        </View>
      )}
      <Pressable
        onPress={handleLogout}
        disabled={loggingOut}
        className="bg-brand-primary rounded-lg px-6 py-3 active:opacity-80 mt-auto flex-row justify-center items-center"
        testID="account-logout"
      >
        {loggingOut ? (
          <ActivityIndicator color="white" />
        ) : (
          <Text className="font-brand-bold text-white text-base">Cerrar sesión</Text>
        )}
      </Pressable>
      <Text className="font-brand text-xs text-brand-dark/50 text-center mt-4">
        Próximamente — Fase 6 (perfil completo, cupones, ayuda)
      </Text>
    </View>
  );
}
```

#### `index.tsx` enriquecido

```tsx
import { View, Text } from 'react-native';
import { useAuthStore } from '@/stores/auth-store';

export default function HomeTab() {
  const user = useAuthStore((s) => s.user);
  
  return (
    <View className="flex-1 items-center justify-center bg-brand-medium p-6">
      <Text className="font-brand-bold text-3xl text-brand-dark mb-2">101tags</Text>
      {user && (
        <Text className="font-brand text-base text-brand-dark mb-4">
          Bienvenido, {user.name}
        </Text>
      )}
      <Text className="font-brand text-sm text-brand-dark/60 text-center">
        Tu tienda de etiquetas en México
      </Text>
    </View>
  );
}
```

## Acceptance Criteria (BDD)

### AC1: 4 Tabs.Screen declaradas

```gherkin
Scenario: Layout tiene 4 tabs
  When render (tabs)/_layout.tsx
  Then existe Tab.Screen "index" con title="Inicio"
  And existe Tab.Screen "catalog" con title="Catálogo"
  And existe Tab.Screen "cart" con title="Carrito"
  And existe Tab.Screen "account" con title="Cuenta"
```

### AC2: Cada tab tiene tabBarIcon (función)

```gherkin
Scenario: Iconos Ionicons
  When render (tabs)/_layout.tsx
  Then tabBarIcon de "index" es función que retorna JSX
  And tabBarIcon de "catalog" es función
  And tabBarIcon de "cart" es función
  And tabBarIcon de "account" es función
```

### AC3: Brand colors en tabBar

```gherkin
Scenario: Colores de marca
  When render (tabs)/_layout.tsx
  Then tabBarActiveTintColor = brandColors.primary
  And tabBarInactiveTintColor = brandColors.dark
  And tabBarStyle.backgroundColor = brandColors.medium
```

### AC4: authGuard funciona

```gherkin
Scenario: Sin sesión → redirect login
  Given isAuthenticated = false
  And isHydrated = true
  When render (tabs)/_layout.tsx
  Then retorna <Redirect href="/(auth)/login" />
```

### AC5: Splash gate

```gherkin
Scenario: Antes de hidratar → null
  Given isHydrated = false
  When render (tabs)/_layout.tsx
  Then retorna null
```

### AC6: Placeholders renderizan

```gherkin
Scenario: Catálogo
  When render catalog.tsx
  Then aparece Text "Próximamente — Fase 2"

Scenario: Carrito
  When render cart.tsx
  Then aparece Text "Próximamente — Fase 3"

Scenario: Cuenta
  When render account.tsx
  Then aparece Text "Mi cuenta"
  And aparece botón "Cerrar sesión"
```

### AC7: Account logout

```gherkin
Scenario: Click logout
  Given user logueado en account tab
  When click "Cerrar sesión"
  Then authService.logout() llamado
  And clearSession() llamado
  And router.replace('/(auth)/login')
```

### AC8: Inicio muestra bienvenida

```gherkin
Scenario: Home con sesión
  Given user = { name: "Juan", email: "..." }
  When render index.tsx
  Then aparece Text "101tags"
  And aparece Text "Bienvenido, Juan"
```

### AC9: package.json tiene dep

```gherkin
Scenario: Dep declarada
  Given package.json
  Then contiene "@expo/vector-icons"
```

## Definition of Done

- [ ] Spec escrita.
- [ ] `@expo/vector-icons` añadido a `package.json`.
- [ ] `(tabs)/_layout.tsx` con 4 tabs + iconos.
- [ ] `catalog.tsx`, `cart.tsx`, `account.tsx` creados.
- [ ] `index.tsx` enriquecido con bienvenida.
- [ ] Tests: `_layout.spec.tsx` (≥5 tests), 3 placeholder specs (≥2 cada uno).
- [ ] `jest.setup.js` mockeando `@expo/vector-icons` (devolver mock component).
- [ ] `pnpm typecheck` exit 0.
- [ ] `pnpm lint` exit 0.
- [ ] Cada test pasa (≥11 nuevos).
- [ ] STATE.md, TASKS.md, spec DONE; commit docs a `developer`.
- [ ] PR contra `developer`, NO merge.

## Mocking de `@expo/vector-icons` en Jest

```js
// jest.setup.js (añadir al final o actualizar)
jest.mock('@expo/vector-icons', () => {
  const React = require('react');
  const { Text } = require('react-native');
  const MockIcon = ({ name, ...props }) => React.createElement(Text, props, name);
  return {
    Ionicons: MockIcon,
    MaterialIcons: MockIcon,
    MaterialCommunityIcons: MockIcon,
    FontAwesome: MockIcon,
    // ... otros si se usan
  };
});
```

Esto evita que los tests intenten cargar fonts reales.

## Riesgos / caveats

1. **`@expo/vector-icons`** trae font al APK (~50KB). Aceptable.
2. **Tabs JS de expo-router 6**: NO nativo. iOS no usa `UITabBarController`, Android no usa `BottomNavigationView`. Aceptado por M1.5.
3. **`tabBarStyle` recibe objetos RN**, no className. Nativewind v4 no soporta style en opciones de Tabs.Screen; usar `tabBarStyle: { backgroundColor }` directamente.
4. **Font scaling**: `tabBarLabel` puede crecer con accessibility. Decisión MVP: respetar (sin `maxFontSizeMultiplier`).
5. **Header por tab**: cada tab screen tendrá header propio (configurado en `_layout.tsx`). Decisión UX: mantener simple.
6. **Tests visuales de iconos**: NO incluidos (requieren device). Tests verifican que `tabBarIcon` es función y retorna JSX mockeado.
7. **Account tab hace logout**: consume `authService.logout()` y `clearSession()` que ya están implementados (M1.4/M1.5).
8. **Tabs en paralelo con #10**: archivos en carpetas distintas (`(auth)/` vs `(tabs)/`), cero conflicto de archivos.

## Handover

Al cerrar:
- ~11 tests nuevos.
- Total tests: 240 → **~258**.
- Issue #11 cerrada en GitHub.
- App móvil: 4 tabs navegables, cada una con icono Ionicons y brand colors. Splash y auth guards ya funcionales desde M1.5/M1.2.
- Próximo: F2.x (catálogo, producto) o continuar issues auditadas si las hay.
