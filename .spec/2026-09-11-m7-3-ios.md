# Spec: M7.3-ios — Signing, universal/deep links y QA (preparación) (#34)

**Status**: PARTIAL / BLOCKED · **Spec ID**: 2026-09-11-m7-3-ios
**Issue**: #34 · **Branch**: `feat/f7` · **Depends**: M7.1

---

## Objetivo

Preparar iOS release (deep links, signing seguro, EAS profile) sin cuentas Apple/certificados/dispositivo.

## Preparado aquí (code/config)

1. **Deep link de reset**: verificar `scheme: "101tags"` + `expo-router`; documentar que el reset link (`101tags://...`) funciona instalado (a validar en M7.3 físico).
2. **Signing seguro**: `.gitignore` cubre certificados/provisioning (`*.p12`, `*.mobileprovision`, `*.cer`, `*.p8`) y `credentials.json`. No commitear perfiles privados.
3. **eas.json** (mismo perfil que Android) con `ios` build config.
4. **Documentar** archive/Xcode y App Store Connect como **BLOQUEADO** (requiere cuenta Apple Developer).

## BLOQUEADO (recursos externos)

- [ ] Build/archive reproducible en Xcode → requiere cuenta Apple Developer + certificados.
- [ ] Reset/deep link funciona instalado → requiere dispositivo/simulador firmado.
- [ ] QA en simulador/dispositivo → requiere entorno Apple.

## Verificación

- JSON válido; `pnpm typecheck`/`lint` exit 0; `.gitignore` con patrones de certificados; ningún certificado trackeado.

## Definition of Done

- [ ] `.gitignore` + `eas.json` + deep link documentado.
- [ ] Criterios bloqueados documentados (no DONE).
